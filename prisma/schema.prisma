generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model preference {
  uid      String    @unique
  darkmode  Boolean   @default(false)
}