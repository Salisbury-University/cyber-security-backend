generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Exercise {
  ID Int @id @default(autoincrement())
  exercise_ID String
  status String
  user String 
}